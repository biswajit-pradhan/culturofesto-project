eureka:
  instance:
    hostname: localhost
server:
  port: 9000
spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      routes:
      - id: admin-service
        uri: lb://admin-service
        predicates:
        - Path=/api/admin/**